{
  "private": true,
  "scripts": {
    "postinstall": "spago install",
    "test": "spago test",
    "build": "spago build",
    "bundle": "spago bundle-app --to dist/app.js",
    "watch": "spago build --watch",
    "bundle:watch": "spago bundle-app --watch --to dist/app.js",
    "bundle:watch-fast": "webpack --mode=development --entry ./entry.js --output-path ./dist --output-filename app.js --progress --watch"
  },
  "devDependencies": {
    "purescript": "^0.13.6",
    "purescript-psa": "^0.7.3",
    "spago": "^0.15.2",
    "webpack": "^4.43.0",
    "webpack-cli": "^3.3.11"
  }
}
